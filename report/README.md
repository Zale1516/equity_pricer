# report/

LaTeX source of the project report (`accumulator_pricing.tex`, English, `pdflatex`). Figures in `fig/`.

```bash
make            # -> accumulator_pricing.pdf  (runs pdflatex twice for refs / ToC)
```

Needs a TeX distribution, e.g. `brew install --cask mactex-no-gui`.
